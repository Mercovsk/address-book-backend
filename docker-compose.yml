version: '3.8'

services:
  server:
    build: .
    container_name: book_address_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - sqlite_data:/app/data
    restart: unless-stopped

volumes:
  sqlite_data: